box: wercker/nodejs
build:
  steps:
    - npm-install
    - arjen/hugo-build:
        version: 0.18
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GITHUB_TOKEN
        domain: yudppp.github.io/blog
        basedir: _public
