box: node
build:
  steps:
    - npm-install
    - arjen/hugo-build:
        version: 0.15
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - lukevivier/gh-pages@0.2.1:
        token: $GITHUB_TOKEN
        domain: yudppp.github.io/blog
        basedir: _public
